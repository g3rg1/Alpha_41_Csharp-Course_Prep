using System;
using System.Collections.Generic;
using System.Linq;

namespace Arrangement
{
    class Program
    {
        static void Main(string[] args)
        {
            var input = Console.ReadLine();
            var numsList = input.Split(' ').Select(i => int.Parse(i)).ToList();
            

        }
    }
}
